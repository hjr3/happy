<!DOCTYPE html>
<html>
<head>
    <title>Happy</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
</head>
<body>

    <header class="container">
        <div class="navbar">
            <div class="navbar-inner">
                <%= link_to 'Happy App', root_path, :class => 'brand' %>
                <ul class="nav">
                    <li><%= link_to 'Thoughts', thoughts_path %></li>
                    <li><%= link_to 'Cheer Me Up!', cheer_up_index_path %></li>
                </ul>
                <% if current_user %>
                    <ul class="nav pull-right">
                        <li><%= link_to 'Logout', logout_path %></li>
                    </ul>
                    <p class="navbar-text pull-right">Hi, <%= current_user.email %></p>
                <% else %>
                    <ul class="nav pull-right">
                        <li><%= link_to 'Signup', signup_path %></li>
                        <li><%= link_to 'Login', login_path %></li>
                    </ul>
                <% end %>
            </div>
        </div>
    </header>
    <%= yield %>

</body>
</html>
